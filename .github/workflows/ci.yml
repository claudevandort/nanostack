name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-test:
    name: build + test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v6

      - name: Set up Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache-dependency-path: tests/conformance/go/go.sum

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: tests/conformance/js/package-lock.json

      - name: Build
        run: zig build

      - name: Unit tests
        run: zig build test

      - name: Install JS conformance deps
        working-directory: tests/conformance/js
        run: npm ci

      - name: Start nanostack
        env:
          NANOSTACK_PORT: '14566'
        run: |
          rm -rf "$RUNNER_TEMP/ns-fs"
          ./zig-out/bin/nanostack --port "$NANOSTACK_PORT" --data-dir "$RUNNER_TEMP/ns-fs" > nanostack.log 2>&1 &
          echo $! > nanostack.pid
          for i in $(seq 1 50); do
            if curl -s -o /dev/null "http://127.0.0.1:${NANOSTACK_PORT}/"; then
              echo "nanostack ready"
              exit 0
            fi
            sleep 0.1
          done
          echo "nanostack failed to start" >&2
          cat nanostack.log >&2
          exit 1

      - name: Go conformance
        working-directory: tests/conformance/go
        env:
          NANOSTACK_ENDPOINT: 'http://127.0.0.1:14566'
          NANOSTACK_BIN: ${{ github.workspace }}/zig-out/bin/nanostack
        run: go test -v -count=1 ./...

      - name: JS conformance
        working-directory: tests/conformance/js
        env:
          NANOSTACK_ENDPOINT: 'http://127.0.0.1:14566'
        run: npm test

      - name: Stop nanostack
        if: always()
        run: |
          if [ -f nanostack.pid ]; then
            kill "$(cat nanostack.pid)" 2>/dev/null || true
            rm -f nanostack.pid
          fi
          if [ -f nanostack.log ]; then
            echo "::group::nanostack.log"
            cat nanostack.log
            echo "::endgroup::"
          fi

  bench:
    name: perf gate (ubuntu-latest)
    runs-on: ubuntu-latest
    needs: build-test
    steps:
      - uses: actions/checkout@v6

      - name: Set up Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache-dependency-path: bench/driver/go.sum

      - name: Run perf gate
        run: zig build bench -- --backend fs --out "$GITHUB_WORKSPACE/bench-result.json"

      - name: Post bench summary
        if: always()
        run: |
          {
            echo "## Perf gate result"
            if [ -f "$GITHUB_WORKSPACE/bench-result.json" ]; then
              echo ""
              echo '```json'
              cat "$GITHUB_WORKSPACE/bench-result.json"
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload bench JSON
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: bench-result
          path: bench-result.json
          if-no-files-found: warn
